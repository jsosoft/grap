/**
* Window
* UI window controller.  
*/
(function()
{
	JSO.Client.Controller.Window = JSO.Client.Controller.extend({
		constructor: JSO.Client.Controller.Window,
		construct: function(model, parent){
			this._super(model, parent);
		}
	});
})();
