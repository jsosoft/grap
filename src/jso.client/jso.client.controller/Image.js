/**
* Image
* Images controller.  Root class for any non-interactive game images
*/
(function()
{
	JSO.Client.Controller.Image = JSO.Client.Controller.Object.extend({
		constructor: JSO.Client.Controller.Image,
		construct: function(model, parent){
			this._super(model, parent);
		}
	});
})();
