/**
* Resource
* Root controller for all application resources
*/
(function()
{
	JSO.Client.Controller.Resource = JSO.Client.Controller.extend({
		constructor: JSO.Client.Controller.Resource,
		construct: function(model, parent){
			this._super(model, parent);
		}
	});
})();
